# System packages required by install.sh (install with apt-get).
# Keep this list synchronized with the APT_PACKAGES array in install.sh.
# apt: python3
# apt: python3-venv
# apt: python3-pip
# apt: python3-dev
# apt: python3-cryptography
# apt: build-essential
# apt: git
# apt: i2c-tools
# apt: python3-smbus
# apt: wireguard-tools
# apt: iproute2
# apt: openssh-client
# apt: sshpass
# apt: rsync
# apt: libssl-dev
# apt: libffi-dev
# Python packages installed into the virtual environment by install.sh.
# cryptography intentionally comes from Debian's python3-cryptography package.
# Trixie provides native armhf/arm64 builds; PyPI does not provide a compatible
# wheel for every Raspberry Pi/Python combination and may otherwise invoke a
# slow Rust/OpenSSL source build during installation.
Adafruit-Blinka
adafruit-circuitpython-bme280
adafruit-circuitpython-bme680
adafruit-circuitpython-sht4x
Flask>=3.0,<4
waitress>=3.0,<4
