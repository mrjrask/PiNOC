# System packages required by install.sh (install with apt-get).
# Keep this list synchronized with the APT_PACKAGES array in install.sh.
# apt: python3
# apt: python3-venv
# apt: python3-pip
# apt: python3-dev
# apt: build-essential
# apt: git
# apt: i2c-tools
# apt: python3-smbus
# apt: libgpiod3
# apt: fonts-dejavu-core
# apt: wireguard-tools
# apt: iproute2
# apt: openssh-client
# apt: sshpass
# apt: rsync
# apt: libopenjp2-7 (OpenJPEG runtime library required by Pillow at import time;
# the package name differs per release, so install.sh installs whichever of
# libopenjp2-7 / libopenjp2-7-1 / libopenjp2-7-2 / libopenjp2-2.3 exists)

# Python packages installed into the virtual environment by install.sh.
Adafruit-Blinka
adafruit-circuitpython-ssd1306
Pillow
displayhatmini
adafruit-circuitpython-bme280
adafruit-circuitpython-bme680
adafruit-circuitpython-sht4x
Flask>=3.0,<4
waitress>=3.0,<4
